# WorkIQ MCP server command (optional - demo data used if not configured)
WORKIQ_COMMAND=npx -y @anthropic-ai/workiq-mcp

# Server port
PORT=3001

# API URL for frontend (defaults to /api with Vite proxy)
VITE_API_URL=/api
